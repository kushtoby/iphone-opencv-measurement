name: Build OpenCV.js (Epipolar) [DISABLED]

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is intentionally disabled."
          echo "We use OpenCV.js CDN + epipolar geometry implemented in pure JS inside index.html."
          echo "Delete this workflow if you don't want it showing up in Actions."
